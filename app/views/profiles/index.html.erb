<p id="notice"><%= notice %></p>

<h1>Profiles</h1>

<table>
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Pool</th>
      <th>Location</th>
      <th>Contact</th>
      <th>About me</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @profiles.first_name %></td>
        <td><%= @profiles.last_name %></td>
        <td><%= @profiles.pool %></td>
        <td><%= @profiles.location %></td>
        <td><%= @profiles.contact %></td>
        <td><%= @profiles.about_me %></td>
        <td><%= link_to 'Show', @profiles %></td>
        <td><%= link_to 'Edit', edit_profile_path(@profiles) %></td>
        <td><%= link_to 'Destroy', @profiles, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  </tbody>
</table>

<br>

<%= link_to 'Logout', destroy_user_session_path, method: :delete %>

<%#= link_to 'New @profiles', new_profiles_path %>
