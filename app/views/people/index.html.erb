<p id="notice"><%= notice %></p>

<h1>People</h1>

<table>
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Pool</th>
      <th>Location</th>
      <th>Contact</th>
      <th>About me</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= @people.first_name %></td>
        <td><%= @people.last_name %></td>
        <td><%= @people.pool %></td>
        <td><%= @people.location %></td>
        <td><%= @people.contact %></td>
        <td><%= @people.about_me %></td>
        <td><%= link_to 'Show', @people %></td>
        <td><%= link_to 'Edit', edit_person_path(@people) %></td>
        <td><%= link_to 'Destroy', @people, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  </tbody>
</table>

<br>

<%= link_to 'Logout', destroy_user_session_path, method: :delete %>

<%#= link_to 'New @people', new_people_path %>
